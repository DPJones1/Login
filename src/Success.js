import React from 'react';

//Success component shows a message when the login has been successful
const Success = () => {

    return React.createElement('div', null,
        React.createElement('h2', null, 'Login Successful')
    );

};

export default Success;